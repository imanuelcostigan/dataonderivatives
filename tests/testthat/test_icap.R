# context("ICAP")
# test_that('ICAP files available', {
#   expect_equal(downloader::download(url = url_icap_zip(TRUE), destfile = tempfile(), quiet = TRUE), 0L)
#   expect_equal(downloader::download(url = url_icap_zip(FALSE), destfile = tempfile(), quiet = TRUE), 0L)
# })
